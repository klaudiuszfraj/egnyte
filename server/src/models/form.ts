export default class Form {
    id:string;
    action:string;
    timestamp:string;
    constructor(id:string, action:string, timestamp:number, ) {
        this.id = id
        this.action = id
        this.timestamp = id
    }
}